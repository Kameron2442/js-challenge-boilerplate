import { Routes } from '@angular/router';
import { PolicyReaderComponent } from './components/pages/policy-reader/policy-reader.component';

export const routes: Routes = [
    {path: '', component: PolicyReaderComponent}
];
